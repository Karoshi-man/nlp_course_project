# Lab 7: Linear SVM & PR-Logic

1. [cite_start]**Підзадача:** Бінарна класифікація (фільтрація) речень з вакансій DOU на дві категорії: містить навички (1) чи ні (0)[cite: 201].
2. [cite_start]**Baseline ЛР6:** Logistic Regression + TF-IDF word(1,2)[cite: 202].
3. [cite_start]**SVM-варіанти:** Протестовано `LinearSVC` виключно зі словами, та комбінований `FeatureUnion` (word(1,2) + char_wb(3,5))[cite: 203].
4. **Дисбаланс:** Був присутній (83.6% на 16.4%). [cite_start]Вирішений через `class_weight='balanced'`[cite: 204].
5. **Поріг:** Зміщено з 0.0 до `-0.5` на Validation Set. [cite_start]Це зроблено під логіку *Recall-first* (максимізація повноти міноритарного класу, щоб не втратити сутності перед NER)[cite: 205].
6. [cite_start]**Найкраща модель:** `LinearSVC` (word+char, balanced) із кастомним порогом `-0.5`[cite: 206].
7. [cite_start]**Що робити далі:** Переходити до задачі Sequence Labeling (NER), оскільки ми успішно відфільтрували релевантні речення, і тепер нам треба точково витягувати самі навички з цих текстів[cite: 207].